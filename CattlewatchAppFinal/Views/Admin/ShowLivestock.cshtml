@model IEnumerable<CattlewatchAppFinal.Models.Livestock>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.livestockName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.livestockName)
        </td>
        <td>
            @*@Html.ActionLink("Delete", "Delete", new { id=item.LivestockId })*@
        </td>
    </tr>
}

</table>